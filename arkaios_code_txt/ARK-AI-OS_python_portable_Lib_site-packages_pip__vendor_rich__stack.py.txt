ORIGINAL FILE: ARK-AI-OS/python_portable/Lib/site-packages/pip/_vendor/rich/_stack.py
--- BEGIN CONTENT ---
from typing import List, TypeVar

T = TypeVar("T")


class Stack(List[T]):
    """A small shim over builtin list."""

    @property
    def top(self) -> T:
        """Get top of stack."""
        return self[-1]

    def push(self, item: T) -> None:
        """Push an item on to the stack (append in stack nomenclature)."""
        self.append(item)

--- END CONTENT ---
