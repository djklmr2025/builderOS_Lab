ORIGINAL FILE: ARK-AI-OS/nuevas propuestas/canjeador.html
--- BEGIN CONTENT ---
<!DOCTYPE html>
<html>
<head>
    <title>ARKAIOS - Canje de Códigos</title>
    <style>
        :root {
            --primary: #64e8aa;
            --secondary: #94b0bb;
            --dark: #1e0033;
            --light: #f4f4f4;
            --success: #28a745;
            --danger: #dc3545;
            --warning: #ffc107;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--dark), var(--primary));
            color: var(--light);
            min-height: 100vh;
            line-height: 1.6;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: white;
        }

        .auth-box {
            text-align: center;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
        }

        .form-group input, 
        .form-group select {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: none;
            background: rgba(0, 0, 0, 0.3);
            color: white;
            font-size: 16px;
        }

        button {
            width: 100%;
            padding: 12px;
            margin-top: 10px;
            border-radius: 8px;
            border: none;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            color: white;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
        }

        button:hover {
            opacity: 0.9;
        }

        .status-message {
            padding: 10px;
            margin: 15px 0;
            border-radius: 8px;
            text-align: center;
            display: none;
        }

        .success {
            background: rgba(40, 167, 69, 0.2);
            border: 1px solid var(--success);
            display: block;
        }

        .error {
            background: rgba(220, 53, 69, 0.2);
            border: 1px solid var(--danger);
            display: block;
        }

        .conversion-card {
            background: linear-gradient(135deg, #2b5876, #4e4376);
            color: white;
            padding: 15px;
            border-radius: 10px;
            margin: 15px 0;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            display: none;
        }

        .rate-display {
            font-size: 1.2rem;
            margin: 10px 0;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            margin-bottom: 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logout-btn {
            background: var(--danger);
            padding: 8px 15px;
            border-radius: 5px;
            cursor: pointer;
        }

        #app-content {
            display: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div id="auth-box" class="auth-box">
            <h1>🔐 Canje de Códigos</h1>
            <div id="status-message" class="status-message"></div>
            
            <div class="form-group">
                <label for="pin-input">PIN de Administrador</label>
                <input type="password" id="pin-input" placeholder="Ingrese su PIN" maxlength="4">
            </div>
            
            <button id="auth-button">Acceder al Sistema</button>
        </div>
        
        <div id="app-content">
            <header>
                <h1>ARKAIOS - Canje AEIO-MR</h1>
                <button id="logout-btn" class="logout-btn">Cerrar Sesión</button>
            </header>
            
            <div class="module-container">
                <h2 class="module-title">Canjear Código</h2>
                <div class="form-group">
                    <label>Seleccionar cuenta:</label>
                    <select id="cuenta-destino">
                        <option value="ARK-024498">ARKAIOS (ARK-024498)</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Código de 16 dígitos:</label>
                    <input type="text" id="codigo-input" placeholder="ARKXXXXXXXXXXXXX" maxlength="16">
                </div>
                <button id="validar-btn" class="btn-primary">Validar Código</button>
                <div id="resultado-validacion" class="conversion-card"></div>
            </div>
        </div>
    </div>

    <script>
        // Configuración inicial
        const ADMIN_PIN = "1234"; // Cambiar esto en producción
        const CODIGOS_VALIDOS = {
            "ARK100ABCDEF1234": { valor: 100, valido: true },
            "ARK500GHIJKL5678": { valor: 500, valido: true },
            "ARK1000MNOPQR9012": { valor: 1000, valido: true }
        };
        
        // Elementos del DOM
        const authBox = document.getElementById('auth-box');
        const appContent = document.getElementById('app-content');
        const pinInput = document.getElementById('pin-input');
        const authButton = document.getElementById('auth-button');
        const statusMessage = document.getElementById('status-message');
        const logoutBtn = document.getElementById('logout-btn');
        const validarBtn = document.getElementById('validar-btn');
        const codigoInput = document.getElementById('codigo-input');
        const resultadoValidacion = document.getElementById('resultado-validacion');
        const cuentaDestino = document.getElementById('cuenta-destino');

        // Mostrar mensaje de estado
        function showStatus(message, type = 'success') {
            statusMessage.textContent = message;
            statusMessage.className = 'status-message ' + type;
            statusMessage.style.display = 'block';
            
            setTimeout(() => {
                statusMessage.style.display = 'none';
            }, 5000);
        }

        // Autenticación
        authButton.addEventListener('click', () => {
            const pin = pinInput.value.trim();
            
            if (pin === ADMIN_PIN) {
                authBox.style.display = 'none';
                appContent.style.display = 'block';
                showStatus('Acceso concedido', 'success');
            } else {
                showStatus('PIN incorrecto', 'error');
                pinInput.value = '';
            }
        });

        // Validar código
        validarBtn.addEventListener('click', () => {
            const codigo = codigoInput.value.trim().toUpperCase();
            const cuenta = cuentaDestino.value;
            
            if (!cuenta) {
                showStatus('Seleccione una cuenta', 'error');
                return;
            }
            
            if (codigo.length !== 16) {
                showStatus('El código debe tener 16 dígitos', 'error');
                return;
            }
            
            if (CODIGOS_VALIDOS[codigo] && CODIGOS_VALIDOS[codigo].valido) {
                resultadoValidacion.innerHTML = `
                    <h3>Código Validado</h3>
                    <div class="rate-display" style="color: var(--success);">✓ VÁLIDO</div>
                    <p>Valor: ${CODIGOS_VALIDOS[codigo].valor} AEIO-MR</p>
                    <p>El crédito ha sido asignado a ${cuenta}</p>
                `;
                resultadoValidacion.style.display = 'block';
                CODIGOS_VALIDOS[codigo].valido = false;
                showStatus('Código canjeado exitosamente', 'success');
            } else {
                resultadoValidacion.innerHTML = `
                    <h3>Código Inválido</h3>
                    <div class="rate-display" style="color: var(--danger);">✗ INVÁLIDO</div>
                    <p>El código no existe o ya fue canjeado</p>
                `;
                resultadoValidacion.style.display = 'block';
                showStatus('Código no válido', 'error');
            }
        });

        // Cerrar sesión
        logoutBtn.addEventListener('click', () => {
            location.reload();
        });
    </script>
</body>
</html>
--- END CONTENT ---
